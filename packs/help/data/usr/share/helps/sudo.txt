
sudo [option] [commands]

  <option>      <commands>      <job>
                mkdir /a        Create a folder in root directory without login as root user
  -a            myaccount       Enable sudo in 'myaccount' user